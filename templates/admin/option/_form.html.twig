{{ form_start (form) }}
{{ form_rest (form) }}
    <button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
    <a href="{{ path ('admin.option.index') }}" class="btn btn-primary">Annuller</a>
{{ form_end (form) }}